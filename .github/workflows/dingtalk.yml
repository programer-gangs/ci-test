name: 钉钉消息发送

on: 
  push:
    branches:
      - main
  workflow_dispatch:


jobs:
  deploy:
    runs-on: ubuntu-latest
    strategy: 
      fail-fast: true
    steps:
      - name: Sending Dingtalk Message
        uses: x-actions/dingtalk@master
        env:
          DINGTALK_ACCESS_TOKEN: 9ed506ea97a847cbfcc00b435f749a30e13cc5585a827332748853a9070ad8bc
          MSGTYPE: markdown
          TITLE: "x-actions dingtalk message"
          TEXT: |
            # Noti
            > Build Github Action Done.
            > ^_^
